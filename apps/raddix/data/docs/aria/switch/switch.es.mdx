---
title: useSwitch
---

# useSwitch

@raddix/switch es un hook que proporciona accesibilidad y comportamiento para un componente switch.

<Demo>
  <Switch />
</Demo>

## Características

- Soporte completo para las funciones del navegador.
- Navegación con teclado completo.
- Soporte de etiquetado para lectores de pantalla.
- API completamente tipificada

## Instalación

Instale la primitiva desde su línea de comando.

```bash
yarn add @raddix/switch
```

## API

### Return value

El hook <code>useSwitch</code> devuelve los siguientes accesorios

<ApiTable
  data={[
    {
      name: 'switchProps',
      description:
        'Un objeto con props de accesibilidad y funcionalidad de un switch.'
    },
    {
      name: 'state',
      description: 'Un objeto que contiene los estados de un switch.'
    }
  ]}
/>

### Parameters

El hook <code>useSwitch</code> acepta un objeto con las siguientes propiedades:

<ApiTable
  data={[
    {
      name: 'checked',
      description: 'Proporciona su propio estado controlado.'
    },
    {
      name: 'onChecked',
      description: 'Este evento actualiza el valor de checked.'
    },
    {
      name: 'disabled',
      description: 'Desabilita toda la funcionalidad del switch.'
    },
    {
      name: 'readOnly',
      description: 'Hace que el switch sea inmutable.'
    },
    {
      name: 'elementType',
      description: 'Cambia el tag HTML elemento que será renderizado.'
    }
  ]}
/>

## Ejemplos

🚧 En progreso
