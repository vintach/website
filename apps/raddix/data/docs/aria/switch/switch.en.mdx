---
title: useSwitch
---

# useSwitch

@raddix/switch is un hook that provides accessibility and behavior for a switch component.

<Demo>
  <Switch />
</Demo>

## Features

- Full support for browser features.
- Full keyboard navigation.
- Labeling support for screen readers.
- Fully-typed API

## Installation

Install the primitive from your command line.

```bash
yarn add @raddix/switch
```

## API

### Return value

El hook <code>useSwitch</code> devuelve los siguientes accesorios

<ApiTable
  data={[
    {
      name: 'switchProps',
      description:
        'An object with accessibility props and functionality of a switch.'
    },
    {
      name: 'state',
      description: 'An object that contains the state of a switch.'
    }
  ]}
/>

### Parameters

El hook <code>useSwitch</code> acepta un objeto con las siguientes propiedades:

<ApiTable
  data={[
    {
      name: 'checked',
      description: 'Provides its own controlled state.'
    },
    {
      name: 'onChecked',
      description: 'This event updates the value of checked.'
    },
    {
      name: 'disabled',
      description: 'Disables all functionality of the switch..'
    },
    {
      name: 'readOnly',
      description: 'Makes the switch immutable.'
    },
    {
      name: 'elementType',
      description: 'Change the HTML element tag that will be rendered.'
    }
  ]}
/>

## Examples

🚧 In progress, come back later
