---
title: Instalación
---

# Instalación

Para utilizar los hooks de Raddix, puedes instalar el paquete completo o instalar los hooks de forma individual.

## Instalación global

Para instalar Raddix de forma global, puedes utilizar el siguiente comando:

```bash
npm install raddix
```

## Instalación individual

Para instalar un hook de Raddix de forma individual, puedes utilizar el siguiente comando:

```bash
npm install @raddix/<hook-name>
```

Por ejemplo si desea instalar el hook `useToggle`, puede utilizar el siguiente comando:

```bash
npm install @raddix/use-toggle
```
