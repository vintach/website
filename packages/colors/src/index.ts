import { purple } from './purple';
import { blue } from './blue';
import { gray } from './gray';

export const colors = {
  black: 'rgb(10, 10, 16)',
  white: 'rgb(240, 240, 240)',
  purple,
  blue,
  gray
};
